

# Web framework & WSGI server
Flask==3.1.2
gunicorn==21.2.0

# HTTP client
requests==2.32.5

# Data validation
pydantic==2.9.2

# Auth DB (Supabase Postgres on Render Free; falls back to SQLite)
psycopg2-binary==2.9.9

# LLM providers
groq==0.37.1
google-generativeai==0.8.5
mistralai==0.0.12
openai>=1.59.0,<2.0.0
cohere>=5.5.0,<6.0.0
huggingface-hub>=0.23.0,<1.0.0

# HTML extraction
beautifulsoup4==4.12.3
lxml>=4.9.0,<6.0.0

# Env vars (local dev only; Render injects env vars natively)
python-dotenv==1.0.1
