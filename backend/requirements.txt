# Web framework & server
Flask==3.0.3
gunicorn==21.2.0

# HTTP & utils
requests==2.32.3

# LLM provider (Groq)
groq>=0.11.0

# Optional: local .env support (Render env vars work without this too)
python-dotenv>=1.0.1
